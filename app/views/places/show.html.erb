<!DOCTYPE html>
<html>
<head>
  <title>Place Details</title>
</head>
<body>
  <h1><%= @place["name"] %></h1>

  <ul>
    <% @entries.each do |entry| %>
      <li>
        <h2><%= entry["title"] %></h2>
        <p><strong>Date:</strong> <%= entry["posted_on"] %></p>
        <p><%= entry["description"] %></p>
      </li>
    <% end %>
  </ul>

  <p>
    <a href="/entries/new?place_id=<%= @place["id"] %>">New Post</a>
  </p>

  <p>
    <a href="/places">Back to Places</a>
  </p>
</body>
</html>